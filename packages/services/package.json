{
  "name": "@assistant/services",
  "version": "0.1.0",
  "description": "Service layer for Academic Assistant (orchestrator, MCP manager)",
  "type": "module",
  "main": "./index.ts",
  "exports": {
    ".": {
      "import": "./index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@assistant/core": "workspace:*",
    "@assistant/infrastructure": "workspace:*",
    "@anthropic-ai/claude-agent-sdk": "^0.2.3"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
